import { PlanetList } from '../starWarComponents';

const PlanetPage = () => {
    return (
        <PlanetList
            onItemSelected={() => { }}
        />
    );
};

export default PlanetPage;
