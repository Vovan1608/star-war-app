import { PersonList } from '../starWarComponents';

const PeoplePage = () => {
    return (
        <PersonList
            onItemSelected={() => { }}
        />
    );
};

export default PeoplePage;
